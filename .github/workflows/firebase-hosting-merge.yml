# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# ... other parts of your workflow file ...

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 # This line should already be there

      # ADD THIS SECTION BELOW actions/checkout@v3
      - uses: subosito/flutter-action@v2 # Use a specific version, e.g., v2
        with:
          flutter-version: '3.32.5' # IMPORTANT: Replace with the Flutter version you are using!
                                  # For example: '3.10.x', '3.13.x', or '3.16.x'
                                  # You can find your version by running `flutter --version` locally.
          channel: 'stable' # Or 'beta', 'dev', depending on your setup

      - name: "Run Flutter Build" # This might be the step that failed
        run: flutter build web --release # Add --release for optimized build
        # ... rest of your workflow
name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STARKTRACKLOG }}
          channelId: live
          projectId: starktracklog
